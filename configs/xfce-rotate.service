[Unit]
Description=Chromebook screen rotation daemon
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/local/bin/xfce-rotate.sh
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
